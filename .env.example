This is an example file.

Copy this to .env (which is in .gitignore) and add your actual secret keys.

Example: If you were using a service like EmailJS or Formspree for your contact form

PUBLIC_CONTACT_FORM_ENDPOINT="your_form_endpoint_url_here"

Example: If you had a project that used a Google Maps API

PUBLIC_GOOGLE_MAPS_API_KEY="your_api_key_here"
